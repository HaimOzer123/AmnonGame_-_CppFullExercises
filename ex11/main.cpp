#include <iostream>

int main(){
    double x = 3;
    double y = 4;
    double z;

    // z = std::max (x, y); // max is not a member of std 
    // z= std::min(x, y); // min is not a member of std
    // z= pow(x, y); // used to raise x to the power of y
    // z= sqrt(x); // used to find the square root of x
    // z= abs(x); // used to find the absolute value of x
    // z= ceil(x); // used to find the smallest integer greater than or equal to x
    // z= floor(x);// used to find the largest integer less than or equal to x
    // z= round(x);// used to find the nearest integer to x
    // z= trunc(x);// used to find the integer part of x
    // z= log(x); // used to find the natural logarithm of x
    // z= log10(x); // used to find the base 10 logarithm of x
    // z= exp(x); // used to find the exponential of x
    // z= sin(x); // used to find the sine of x
    // z= cos(x);// used to find the cosine of x
    // z= tan(x);   // used to find the tangent of x
    // z= asin(x);  // used to find the arcsine of x
    // z= acos(x);// used to find the arccosine of x
    // z= atan(x);// used to find the arctangent of x
    // z= atan2(x, y); // used to find the arctangent of x/y
    // z= sinh(x);// used to find the hyperbolic sine of x
    // z= cosh(x);// used to find the hyperbolic cosine of x
    // z= tanh(x);// used to find the hyperbolic tangent of x
    // z= asinh(x);// used to find the hyperbolic arcsine of x
    // z= acosh(x);// used to find the hyperbolic arccosine of x
    // z= atanh(x);// used to find the hyperbolic arctangent of x
    // z= fmod(x, y);// used to find the remainder of x/y
    // z= remainder(x, y);// used to find the remainder of x/y
    // z= copysign(x, y);// used to copy the sign of y to x
    // z= nextafter(x, y);// used to find the next representable value of x in the direction of y
}